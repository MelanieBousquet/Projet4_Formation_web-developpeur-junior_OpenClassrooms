{% extends "admin/layout.html.twig" %}

{% block body %}

<div id="page-wrapper">
      <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Suppression commentaire</h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <p>Etes-vous certain de vouloir supprimer ce commentaire ?</p>

  <form action="{{ path('ebook_blog_comment_delete', {'id': comment.id}) }}" method="post">
    <a href="{{ path('ebook_blog_comment', { 'id': comment.id }) }}" class="btn btn-default">
      <i class="glyphicon glyphicon-chevron-left"></i>
      Retour au commentaire
    </a>
    <input type="submit" value="Supprimer" class="btn btn-danger" />
    {# Ceci va générer le champ CSRF #}
    {{ form_rest(form) }}
  </form>

</div>
<!-- /#page-wrapper -->

{% endblock %}
